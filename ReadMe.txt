#vue environment setting
> To install, move folder
> vue create '이름' --no-git
> npm i -g @vue/cli
> npm install : package.json에 작성된 의존패키지 설치
> npm run serve
> npm run build : 빌드작업만
> npm run 

#기타
> 노드 버전 : node -v
> npm 버전 : npm -v
> vue --version  // 3.버전
> 뷰설치admin  npm i -g vue-cli 또는 npm install vue-cli -global 
> 뷰삭제admin  npm uninstall -g vue-cli


# vscode 플러그인
> Auto close Tag
> ESLint
> Live Server
> Prettier - Code formatter
> Vetur

#issue
> eslint 포맷 에러 콘피그 설정 필요
> eslint --init
> npm install -g eslint

#0713 ESLint : 너무어려움 ..삭제
#'vue-cli-service'은(는) 내부 또는 외부 명령, 실행할 수 있는 프로그램, 또는
배치 파일이 아닙니다. : 새로 프로젝트 임포트했을때 node moule 없음... npm i